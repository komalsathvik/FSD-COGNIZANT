# Task 2 - Bootstrap Structure and Files

Low-level folder purpose:

- `css`: compiled Bootstrap styles such as `bootstrap.min.css`.
- `js`: Bootstrap JavaScript plugins. Use `bootstrap.bundle.min.js` because it includes Popper.
- `icons`: Bootstrap Icons SVG/font files when the icons package is downloaded.

The sample page includes Bootstrap CSS and the bundled JavaScript plugin file.
